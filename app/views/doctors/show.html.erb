<h2><%= @doctor.name %></h2>
<h3>Department: <%=@doctor.department%></h3>
Appointments:
<ul>
  <% @doctor.appointments.each do |app| %>
    <li><%=app.appointment_datetime.strftime("%B %e, %Y at %H:%M")%> - <%= link_to app.patient.name, patient_path(app.patient) %></li> 
  <% end %> 
</ul>